# WelloWork

WelloWork marketing website — Next.js 16 App Router, React 19, TypeScript (strict).

## Develop

```bash
npm run dev     # start the dev server
npm run build   # production build
npm run lint    # eslint
```

## Docs

- [Internationalization (i18n) & localization](docs/i18n.md) — locales, routing,
  the `proxy.ts` redirect rules, the message catalogs, adding/translating a locale,
  and the SEO-guideline mapping.
- Project conventions and content rules live in [`AGENTS.md`](AGENTS.md).
